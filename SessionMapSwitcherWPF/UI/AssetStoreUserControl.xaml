<UserControl x:Class="SessionModManagerWPF.UI.AssetStoreUserControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SessionModManagerWPF.UI"
             mc:Ignorable="d" 
             d:DesignHeight="550" d:DesignWidth="750">
    <UserControl.Resources>
        <Style TargetType="CheckBox">
            <Setter Property="Margin" Value="0,0,0,0"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource MainButtonStyle}"/>
    </UserControl.Resources>
    
    <Grid Background="LightGray">
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>


        <GroupBox Grid.Row="0" Grid.Column="0"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="5,0,0,5"
                  Header="Categories" Padding="3,10,0,10">
            <WrapPanel>
                <CheckBox Content="All" Padding="05,0,35,5"/>
                <CheckBox Content="Maps" Padding="5,0,10,0"/>
                <CheckBox Content="Decks" Padding="5,0,10,0"/>
                <CheckBox Content="Griptapes" Padding="5,0,7,0"/>
                <CheckBox Content="Trucks" Padding="5,0,10,0"/>
                <CheckBox Content="Wheels" Padding="5,0,10,0"/>
                <CheckBox Content="Hats" Padding="5,0,15,0"/>
                <CheckBox Content="Shirts" Padding="5,0,12,0"/>
                <CheckBox Content="Pants" Padding="5,0,30,0"/>
                <CheckBox Content="Shoes" Padding="5,0,10,0"/>
            </WrapPanel>
        </GroupBox>
        
        
        <ListView Grid.Row="1" Grid.Column="0"
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="5,10,0,10">
            
        </ListView>

        <GroupBox Grid.Row="1" Grid.Column="1" 
                  HorizontalAlignment="Stretch"
                  VerticalAlignment="Stretch"
                  Margin="5,0,5,10"
                  Header="Selected Asset Preview">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="auto"/>
                </Grid.RowDefinitions>

                <StackPanel Grid.Row="0" Margin="0,0,0,10">
                    <WrapPanel Margin="0,0,0,0">
                        <TextBlock Text="Author:"
                                   Margin="5,5,5,0"
                                   FontWeight="Bold"/>
                        <TextBlock Text="rodriada000"
                                   TextWrapping="Wrap"
                                   Margin="5,5,0,0"/>
                    </WrapPanel>

                    <WrapPanel Margin="0,0,0,0">
                        <TextBlock Text="Description:" 
                                   Margin="5,5,5,0"
                                   FontWeight="Bold"/>
                        <TextBlock Text="This is a description of the asset enjoy this thing we have here with my long description"
                                   TextWrapping="Wrap"
                                   Margin="5,5,0,0"/>
                    </WrapPanel>
                </StackPanel>

                
                

                <Image Margin="5,0,5,5"
                       HorizontalAlignment="Stretch"
                       VerticalAlignment="Stretch"
                       Grid.Row="1"
                       RenderOptions.BitmapScalingMode="HighQuality"/>

                <Button Content="Install Asset"
                        Padding="5"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Bottom"
                        Grid.Row="2"/>
            </Grid>
        </GroupBox>

        <TextBlock Grid.Row="2" Grid.ColumnSpan="2"
                   VerticalAlignment="Bottom"
                   HorizontalAlignment="Left"
                   FontSize="13"
                   Margin="5,0,0,5"
                   TextWrapping="Wrap"
                   Text="This is a message to the user ..."/>
    </Grid>
</UserControl>
